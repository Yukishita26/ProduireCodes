ベクトルとは
	整数である次元を持つ
	【中身：倍浮動小数の配列】を持つ
	はじめ（データ）の手順
		中身=データ
		次元=データの個数
	終わり
	
	【自分】から第，【ｎ：整数】成分を，取得する手順：倍浮動小数
		中身（ｎ）を返す
	終わり
	
	【自分】と，［他のベクトル：ベクトル］の，和を求める手順：ベクトル
		もし次元と（他のベクトルの次元）が等しくないなら「次元が異なります」というエラーを発生させる
		データ=｛｝
		次元回，［指数］にカウントしながら繰り返す
			（中身の指数番目）と（他のベクトルの中身の指数番目）の和を，データへ追加する
		繰り返し終わり
		ベクトル（データ）を作ったものを返す
	終わり
	
	【自分】と，【スカラー：倍浮動小数】の，スカラー倍を求める手順：ベクトル
		データ=｛｝
		中身のすべての要素について『
			要素とスカラーの積をデータへ追加する
		』ことをそれぞれ繰り返す
		ベクトル（データ）を作ったものを返す
	終わり

	【自分】と，［他のベクトル：ベクトル］の，要素積を求める手順：ベクトル
		もし次元と（他のベクトルの次元）が等しくないなら「次元が異なります」というエラーを発生させる
		データ=｛｝
		次元回，［指数］にカウントしながら繰り返す
			（中身の指数番目）と（他のベクトルの中身の指数番目）の積を，データへ追加する
		繰り返し終わり
		ベクトル（データ）を作ったものを返す
	終わり
	
	【自分】と，【他のベクトル：ベクトル】の，内積を求める手順：倍浮動小数
		自分と他のベクトルの要素積の中身の合計を返す
	終わり
	
	【自分】の，ノルムを求める手順：倍浮動小数
		自分と自分の内積の平方根を返す
	終わり

	【自分】の，文字列を求める手順：文字列
		中身を「，」で繋げたものを「（」と「）」で囲んだものを返す
	終わり
	
	【自分】を，表示する手順
		（「ベクトル」&自分の文字列）を表示する
	終わり
終わり

ベクトルをテストする手順
	ｘ=ベクトル（｛1，2，3｝）を作ったもの
	ｙ=ベクトル（｛3，4，5｝）を作ったもの
	ｃ=3
	「x+y：［xとyの和の文字列］」を表示する
	「x*y：［xとyの要素積の文字列］」を表示する
	「x・y：［xとyの内積］」を表示する
	「3x：［xと3のスカラー倍の文字列］」を表示する
	「「x[2]：」」&（xから第2成分を取得）を表示する
	「|x|：［xのノルム］」を表示する
終わり

ベクトルをテストする
